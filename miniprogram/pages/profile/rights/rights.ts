Page({
  data: {
    // Currently no data needed for empty state
  },

  onLoad() {
    // Could load balance details from server/storage in the future
  },

  navigateBack() {
    wx.navigateBack();
  }
}); 